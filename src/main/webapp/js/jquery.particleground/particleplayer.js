/**
 * Particleground demo
 * @author RudolphLiu
 */

$(document).ready(function() {
  $('#particles').particleground({
    dotColor: '#B9D3EE',
    lineColor: '#DEDEDE',
    maxSpeedX: '2.5',
    density:9500,
    particleRadius:'8',
    parallaxMultiplier: '20',
  });
});